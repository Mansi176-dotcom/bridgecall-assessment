# Core app and deterministic evaluations work without a key.
OPENAI_API_KEY=
ASR_MODEL=gpt-4o-mini-transcribe
PORT=8765

# Optional offline model; install requirements-audio.txt first.
ASR_PROVIDER=openai
LOCAL_ASR_MODEL=base
ASR_MODEL_CACHE=.model-cache
